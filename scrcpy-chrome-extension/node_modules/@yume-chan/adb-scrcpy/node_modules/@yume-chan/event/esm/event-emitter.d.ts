import type { Disposable } from "./disposable.js";
import type { Event, EventListener, RemoveEventListener } from "./event.js";
export interface EventListenerInfo<TEvent> {
    listener: EventListener<TEvent, unknown, unknown[]>;
    thisArg: unknown;
    args: unknown[];
}
export declare class EventEmitter<TEvent> implements Disposable {
    protected readonly listeners: EventListenerInfo<TEvent>[];
    constructor();
    protected addEventListener(info: EventListenerInfo<TEvent>): RemoveEventListener;
    event: Event<TEvent>;
    fire(e: TEvent): void;
    dispose(): void;
}
//# sourceMappingURL=event-emitter.d.ts.map