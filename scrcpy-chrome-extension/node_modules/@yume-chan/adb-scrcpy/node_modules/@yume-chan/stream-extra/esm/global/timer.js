import { getGlobalValue } from "./utils.js";
export const setTimeout = getGlobalValue("setTimeout") ?? (() => { });
export const clearTimeout = getGlobalValue("clearTimeout") ?? (() => { });
//# sourceMappingURL=timer.js.map