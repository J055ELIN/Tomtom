import { getUint32BigEndian, getUint64BigEndian, } from "@yume-chan/no-data-view";
import { BufferedTransformStream, TransformStream, } from "@yume-chan/stream-extra";
export function createMediaStreamTransformer(options) {
    // Quick path for raw stream without frame meta
    if (!options.sendFrameMeta) {
        return new TransformStream({
            transform(chunk, controller) {
                controller.enqueue({
                    type: "data",
                    data: chunk,
                });
            },
        });
    }
    return new BufferedTransformStream(async (buffered) => {
        const header = await buffered.readExactly(12);
        if (header[0] & 0x80) {
            return {
                type: "session",
                isClientResize: !!(header[0] & 1),
                width: getUint32BigEndian(header, 4),
                height: getUint32BigEndian(header, 8),
            };
        }
        if (header[0] & 0x40) {
            return {
                type: "configuration",
                data: await buffered.readExactly(getUint32BigEndian(header, 8)),
            };
        }
        return {
            type: "data",
            keyframe: !!(header[0] & 0x20),
            pts: getUint64BigEndian(header, 0) & 0x1fffffffffffffffn,
            data: await buffered.readExactly(getUint32BigEndian(header, 8)),
        };
    });
}
//# sourceMappingURL=media-stream-transformer.js.map