import { struct, u32, u8 } from "@yume-chan/struct";
export const ScrcpyInjectKeyCodeControlMessage = struct({
    // value of `type` can change between versions
    type: u8,
    action: u8(),
    keyCode: u32(),
    repeat: u32,
    metaState: u32(),
}, { littleEndian: false });
//# sourceMappingURL=inject-key-code.js.map