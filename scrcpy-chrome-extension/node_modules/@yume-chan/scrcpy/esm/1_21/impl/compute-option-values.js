import { PrevImpl } from "./prev.js";
export function overrideClipboardAutosync(value) {
    if (!value.control) {
        value.clipboardAutosync = false;
    }
}
export function computeOptionValues(options, defaults) {
    const value = PrevImpl.computeOptionValues(options, defaults);
    overrideClipboardAutosync(value);
    return value;
}
//# sourceMappingURL=compute-option-values.js.map