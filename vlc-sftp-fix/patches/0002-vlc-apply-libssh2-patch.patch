--- a/contrib/src/ssh2/rules.mak
+++ b/contrib/src/ssh2/rules.mak
@@ -20,6 +20,7 @@ $(TARBALLS)/libssh2-$(LIBSSH2_VERSION).tar.xz:
 
 ssh2: libssh2-$(LIBSSH2_VERSION).tar.xz .sum-ssh2
 	$(UNPACK)
+	$(APPLY) $(SRC)/ssh2/0001-libssh2-gcrypt-rsa-sha2.patch
 	$(MOVE)
 
 DEPS_ssh2 = gcrypt $(DEPS_gcrypt)
